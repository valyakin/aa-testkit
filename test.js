// deployAA(aa_code);
// const alice = await genAddress(10e9); // gen a new random address and send 10 GB to it from the witness
// const bob = await genAddress(40e9);
// const carol = await genAddress(100e9);
// await sendTransaction({asset, amount, from: alice, aa_address, data_payload: {…}});
// await witness(3); // have the witness post 3 transactions to get something confirmed
// await sendTransaction({asset: asset2, amount: amount2, from: bob, aa_address, data_payload: {…}});
// await sendTransaction({asset: asset3, amount: amount3, from: carol, aa_address, data_payload: {…}});
// await witness(2);
// await readStateVar(…); // and compare with something
